# Required for Electron Forge packaging
# Ensures node_modules uses a flat structure for Forge's module resolution
node-linker=hoisted

# Disable lifecycle script security prompts (trust all build scripts)
enable-pre-post-scripts=true

# Workspace configuration
# Keep separate lockfiles for each package (isolated dependencies)
shared-workspace-lockfile=false
